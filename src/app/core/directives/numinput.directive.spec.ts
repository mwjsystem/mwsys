import { NuminputDirective } from './numinput.directive';

describe('NuminputDirective', () => {
  it('should create an instance', () => {
    const directive = new NuminputDirective();
    expect(directive).toBeTruthy();
  });
});
